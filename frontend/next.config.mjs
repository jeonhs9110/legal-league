/** @type {import('next').NextConfig} */
const nextConfig = {
  // No remote image or media hosts: every asset is first-party or public domain,
  // so nothing third-party is fetched at runtime. See LICENSES.md.
};

export default nextConfig;
